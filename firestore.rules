rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Disabled all security for public production testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
